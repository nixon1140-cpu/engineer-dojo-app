import type { Track } from '../../types'

// AI活用開発演習トラック
// 実AIを使わず、疑似AI出力レビュー・プロンプト設計ワークシート形式で学ぶ
export const aiDevTracks: Track[] = [
  {
    id: 'ai-dev',
    title: 'AI活用開発演習',
    category: 'tech',
    color: 'violet',
    icon: 'fa-wand-magic-sparkles',
    tagline: 'AIを「ツール」として正しく使いこなす',
    description:
      '生成AIの基本的な使い方から、AIが書いたコードのレビュー・修正、そしてリスク管理まで。実AIは使わず、疑似AI出力のレビュー演習とプロンプト設計の自己採点で実践的なAI活用スキルを身につける。',
    outcomes: [
      '効果的なプロンプトの構造を理解し、要件を明確に言語化できる',
      'AIが生成したコードを読んで問題点を発見・修正できる',
      'ハルシネーション・セキュリティリスクを意識してAI出力を検証できる',
    ],
    chapters: [
      {
        id: 'ai-dev-basics',
        title: '第1章: AI活用開発の基礎',
        description: '生成AIをエンジニアの道具として使いこなすための基礎知識と実践演習。',
        lessons: [
          // ========== L1: 生成AIの基本と正しい使い方 ==========
          {
            id: 'aid-1-1',
            title: '生成AIの基本と正しい使い方',
            minutes: 20,
            intro:
              'ChatGPT・GitHub Copilot・Claude などの生成AIは、エンジニアの「強力な補助ツール」です。しかし「なんとなく聞く」と「質の低い回答」が返ってきます。プロンプトの設計次第で、出力の品質は劇的に変わります。このレッスンではプロンプトの基本構造と「効くプロンプト」の作り方を学びます。',
            content: [
              '**生成AIとは何か**: 大量のテキストを学習した言語モデルが、「次に来る可能性が高い言葉」を確率的に生成するシステムです。「理解して答える」のではなく「もっともらしい文章を生成する」ため、間違いも自信満々に出力します。',
              '**プロンプトの4要素**: ①**役割（Role）**「あなたはTypeScriptのエキスパートです」→ 出力のトーンと専門性を定める。②**コンテキスト（Context）**「このコードはNode.js 18で動く在庫管理システムです」→ 背景を渡す。③**指示（Instruction）**「以下のバグを修正してください」→ 何をしてほしいか明確に。④**制約（Constraint）**「コメントは日本語で。既存の関数シグネチャは変えないこと」→ 出力の形式・制限を指定。',
              '**「曖昧なプロンプト」vs「明確なプロンプト」の違い**: ❌ `「このコードを直して」` は情報が何もない。✅ `「Node.js 18で動くTypeScriptコードです。inputがnullのときにTypeErrorが出ます。nullチェックを追加して修正してください。戻り値の型は変えないでください。」` は4要素が揃っている。',
              '**反復改善（Iteration）**: 最初のプロンプトで完璧な答えが出ることはまれです。「もっと簡潔にして」「〇〇の部分は正しいが、△△の部分が違う。修正して」のように、会話の中で精度を上げていくのがコツです。AIは「直前の文脈を覚えている」ので、同じ会話内でフォローアップするのが効率的です。',
              '**プロンプト設計の自己採点ポイント**: 役割は指定したか？ 言語・バージョン等の環境情報を渡したか？ 「何をしてほしいか」が1文で言えるか？ 出力の形式（コードのみ・説明つき・日本語で等）を指定したか？',
            ],
            points: [
              'プロンプト4要素: 役割・コンテキスト・指示・制約を揃える',
              '曖昧な指示は曖昧な出力になる。「何を」「どんな条件で」を明確に',
              '1回で完璧を目指さない。反復改善で精度を上げる',
              'AIは「確率的に生成」する。正しそうでも検証が必要',
            ],
            quiz: {
              question:
                '以下のプロンプト「このAPIのエラーを直して」に最も欠けている要素はどれ？',
              hint: 'AIは「このAPI」が何の言語で書かれているか、どんなエラーが出ているかを知りません。',
              options: [
                {
                  text: '役割（Role）の指定',
                  correct: false,
                  why: '役割は出力のトーンを整えるために有効ですが、このプロンプトで最も致命的に欠けているのは別の情報です。',
                },
                {
                  text: 'コンテキスト（言語・エラー内容・コード）の不足',
                  correct: true,
                  why: '正解です！「このAPI」とは何か、どんな言語・フレームワークか、どんなエラーメッセージが出ているか、コードはどこかが全く分かりません。AIは「提供された情報だけで答える」ので、コンテキストが命です。',
                },
                {
                  text: '制約（Constraint）の不足',
                  correct: false,
                  why: '制約（出力形式の指定など）も重要ですが、「このAPI」と言われても何も情報がない状態が最大の問題です。',
                },
              ],
            },
            codeExercise: {
              prompt:
                '以下は「生成AIに投げたプロンプト例」です。プロンプト設計の4要素（役割・コンテキスト・指示・制約）の観点から評価してください。',
              code: `/* === あなたが評価するプロンプト === */

「TypeScriptのエキスパートとして回答してください（役割）。
 以下は受注管理システムのNode.js 18 + TypeScriptコードです（コンテキスト）。
 order.statusが'pending'のとき、処理が中断せずに次に進んでしまいます。
 statusが'pending'のとき早期リターンするよう修正してください（指示）。
 既存の関数シグネチャは変えず、修正箇所にコメントをつけ、日本語で説明を1段落添えてください（制約）。」

/* === 実際に返ってきたAIの出力（疑似）=== */

function processOrder(order: Order): void {
  // 追加: pendingの注文は処理しない
  if (order.status === 'pending') {
    console.log('注文は保留中です。処理をスキップします。')
    return
  }
  // 既存の処理
  chargePayment(order)
  updateInventory(order)
  sendConfirmationEmail(order)
}
// 修正説明: statusが'pending'の場合、処理の先頭で早期リターンするよう変更しました。
// これにより、保留中の注文に対して決済・在庫更新・メール送信が実行されることを防ぎます。`,
              question: 'このプロンプトとAI出力の組み合わせを評価するとき、最も正確な評価はどれ？',
              hint: '4要素が揃っているか、そしてAIの出力が指示通りか確認してみましょう。',
              options: [
                {
                  text: 'プロンプトは4要素が揃っており、AI出力も指示通りに修正されている。このまま採用してよい。',
                  correct: false,
                  why: '「このまま採用」は早急です。コードが正しく動くか、他の箇所への影響がないかはテストで確認が必要です。AIの出力が「指示通り」でも、「バグがない」の保証にはなりません。',
                },
                {
                  text: 'プロンプトは4要素が揃っており良質。AI出力も指示通りだが、テスト（例: pendingで処理が止まるか、approved で正常に進むか）で動作確認してから採用すべき。',
                  correct: true,
                  why: '正解です！プロンプトはよく設計されています。出力も指示を満たしています。しかし「正しそう」と「テスト済み」は別物。pendingで早期リターンするか、approved で全処理が走るかをテストで確認してから採用するのがプロのやり方です。',
                },
                {
                  text: 'プロンプトに役割と制約が混在しており設計が悪い。プロンプトを作り直すべき。',
                  correct: false,
                  why: '役割・コンテキスト・指示・制約は1つのプロンプトにまとめて書くのが一般的です。このプロンプトは適切に4要素を含んでいます。',
                },
              ],
            },
          },

          // ========== L2: AIでコードを書く実践 ==========
          {
            id: 'aid-1-2',
            title: 'AIでコードを書く実践',
            minutes: 25,
            intro:
              '「AIにコードを書かせる」サイクルは「要件の言語化 → プロンプト設計 → コード生成 → 動作確認 → 改善」の繰り返しです。このレッスンでは、AIが生成した「バグありコード」のレビューと修正を通じて、このサイクルの「確認」フェーズを重点的に訓練します。',
            content: [
              '**AI活用開発の5ステップサイクル**: ①**要件言語化** — 「何が欲しいか」を具体的に書き出す（曖昧さをゼロにする）。②**プロンプト設計** — 4要素（役割・コンテキスト・指示・制約）を揃える。③**コード生成** — AIに投げて出力を得る。④**動作確認** — テストを実行し、エッジケースを試す。⑤**改善** — バグが出たら原因を伝えて再生成・手修正。',
              '**AIコードレビューの観点**: ❶**動作の正確さ** — 指示した要件を満たしているか。❷**エッジケース** — 空配列・null・0・境界値で壊れないか。❸**セキュリティ** — SQLインジェクション・XSSなどの脆弱性がないか（L3で詳しく学ぶ）。❹**可読性** — 変数名・コメントが分かりやすいか。❺**既存コードとの整合性** — チームのコーディング規約・命名規則に合っているか。',
              '**よくある「AIコードの落とし穴」パターン**: ▶ エラーハンドリングが省略されている（try-catchなし）▶ 非同期処理でawaitを忘れている ▶ 入力値の検証（バリデーション）がない ▶ ハードコードされた値（URLや設定値を直書き）▶ 古い書き方（deprecated APIの使用）',
              '**「コードが動いた≠完成」**: AIが書いたコードは「ハッピーパス（正常系）」だけ動くことが多いです。「空配列を渡したら？」「nullが来たら？」「リクエストが失敗したら？」を試してこそ、本番コードと言えます。',
            ],
            points: [
              'AIコード活用は「要件言語化→プロンプト→生成→確認→改善」の5ステップサイクル',
              'AIはハッピーパスしか考えないことが多い。エッジケースのテストは自分でやる',
              'エラーハンドリング・バリデーション・非同期のawait漏れに注意',
              '「動いた」はスタート。テスト済みになってはじめて「完成」',
            ],
            codeExercise: {
              prompt: `以下はAIが生成した「ユーザーの年齢確認関数」のコードです。
プロンプト指示は「ユーザーオブジェクトを受け取り、18歳以上ならtrueを返すisAdult関数を書いて」でした。
このAI出力コードのレビューをしてください。`,
              code: `/* === AIが生成したコード（疑似出力）=== */

function isAdult(user) {
  return user.age >= 18
}

/* === AIが生成したテストコード（疑似出力）=== */
// テスト1: 20歳ならtrue
console.log(isAdult({ age: 20 }))  // → true ✅
// テスト2: 15歳ならfalse
console.log(isAdult({ age: 15 }))  // → false ✅

/* === あなたが追加検証したエッジケース === */
console.log(isAdult(null))          // → TypeError: Cannot read properties of null
console.log(isAdult({}))            // → false (undefined >= 18 は false になる)
console.log(isAdult({ age: '20' })) // → true  (文字列との比較が意図しない挙動を生む可能性)`,
              question: 'このAI出力のコードに存在する最も重大な問題はどれ？',
              hint: 'AIが書いたテストは「動く入力」だけです。実際の呼び出し元からはどんな値が来るか分かりません。',
              options: [
                {
                  text: '関数名が不明確なので rename すべき',
                  correct: false,
                  why: '`isAdult` は「成人かどうか」を表す良い名前です。命名は問題ではありません。',
                },
                {
                  text: 'null / undefined / 型違いの入力を検証していないため、実際の呼び出しでエラー・誤動作が起きる',
                  correct: true,
                  why: '正解です！nullが来るとTypeError、ageキーがないと「undefined >= 18 → false」になってしまいます。本番コードでは if (!user || typeof user.age !== \'number\') return false のような入力チェックが必須です。AIのテストは「正常系しか試さない」ことが多いので、レビュー側がエッジケースを加える責任があります。',
                },
                {
                  text: 'TypeScriptを使っていないので型安全ではない',
                  correct: false,
                  why: 'TypeScript化は推奨されますが、このコードの最大問題はnull/undefinedで即クラッシュする「実行時エラー」です。JavaScriptのままでも入力チェックで解決できます。',
                },
              ],
            },
            codingChallenge: {
              prompt: `上のAI出力コードをレビューした結果、null・ageなし・ageが数値でない場合のエッジケースが未対応でした。
以下の要件を満たすよう isAdult 関数を修正してください。

要件:
- userがnullまたはundefinedの場合はfalseを返す
- user.ageが存在しない（undefined）場合はfalseを返す  
- user.ageが数値でない場合はfalseを返す
- user.ageが18以上の場合はtrueを返す
- それ以外（18未満）はfalseを返す`,
              functionName: 'isAdult',
              signature: 'function isAdult(user)',
              starterCode: `function isAdult(user) {
  // TODO: エッジケースのチェックを追加してください
  // 1. userがnullまたはundefinedの場合
  // 2. user.ageがundefinedの場合
  // 3. user.ageが数値でない場合
  // 4. 正常系（18歳以上かどうかの判定）
  return user.age >= 18
}`,
              tests: [
                {
                  description: 'null を渡すと false',
                  script: 'isAdult(null)',
                  expected: 'false',
                },
                {
                  description: 'undefined を渡すと false',
                  script: 'isAdult(undefined)',
                  expected: 'false',
                },
                {
                  description: 'ageキーなし({}) は false',
                  script: 'isAdult({})',
                  expected: 'false',
                },
                {
                  description: 'ageが文字列 "20" は false（型ミス）',
                  script: 'isAdult({ age: "20" })',
                  expected: 'false',
                },
                {
                  description: '20歳は true',
                  script: 'isAdult({ age: 20 })',
                  expected: 'true',
                },
                {
                  description: '18歳は true（境界値）',
                  script: 'isAdult({ age: 18 })',
                  expected: 'true',
                },
                {
                  description: '17歳は false（境界値）',
                  script: 'isAdult({ age: 17 })',
                  expected: 'false',
                },
              ],
              hints: [
                'まず `if (!user)` でnull/undefinedをまとめてチェックできます',
                '`typeof user.age !== \'number\'` でageが数値かチェックできます',
                '2つのチェックを `||` で組み合わせて早期リターンしましょう',
              ],
              solution: `function isAdult(user) {
  // エッジケース: userがnull/undefinedの場合、またはageが数値でない場合
  if (!user || typeof user.age !== 'number') return false
  // 正常系: 18歳以上かどうか
  return user.age >= 18
}`,
            },
          },

          // ========== L3: AI出力のリスク管理 ==========
          {
            id: 'aid-1-3',
            title: 'AI出力のリスク管理',
            minutes: 20,
            intro:
              'AIはコードを生成しますが、「正確さ」「最新性」「セキュリティ」を保証しません。このレッスンではAI出力の3大リスク（ハルシネーション・古い情報・セキュリティ脆弱性）を学び、AIが書いたコードを本番に出す前のチェックポイントを身につけます。',
            content: [
              '**ハルシネーション（幻覚）とは**: AIが「もっともらしいが間違い」の情報を自信満々に生成する現象。存在しないAPIメソッド名・誤った仕様・架空のライブラリなどが代表例。「エラーになった」「ドキュメントに載っていない」ときはハルシネーションを疑う。',
              '**古い情報の問題**: 言語モデルの学習データには「カットオフ日」があります。Node.jsのLTSバージョン・ライブラリのAPIの破壊的変更・廃止された書き方などは、AIが古いバージョン基準で答えることがあります。バージョン依存の情報は必ず公式ドキュメントで確認する。',
              '**AIが生成するセキュリティリスク（代表例）**: ❶**SQLインジェクション** — `"SELECT * FROM users WHERE id = " + userId` のように直接文字列結合するパターン。❷**ハードコードされた秘密情報** — APIキー・パスワードをコードに直書き。❸**入力値無検証** — フォームやリクエストボディの値を検証せず直接使う。❹**エラーの露出** — スタックトレースやDB情報をそのままレスポンスに返す。',
              '**AI出力のチェックポイント10項目**: ①APIメソッドは実在するか（公式ドキュメントで確認）②ライブラリバージョンが合っているか ③SQLはプレースホルダーを使っているか ④APIキー・パスワードが直書きされていないか ⑤ユーザー入力を検証しているか ⑥エラーメッセージが外部に漏れないか ⑦エッジケース（null/空/境界値）を試したか ⑧非同期処理でawaitが漏れていないか ⑨TypeScriptならany型の多用がないか ⑩既存コードの命名規則・スタイルに合っているか',
              '**「AIはコパイロット（副操縦士）」の心構え**: 飛行機のコパイロットは操縦を補助しますが、最終判断は機長がします。AIもコードを提案しますが、それが正しいかの最終判断はあなたです。AIを使う時間分、検証スキルへの投資が重要になります。',
            ],
            points: [
              'ハルシネーション: AIは「もっともらしい間違い」を生成する。エラーになったら真っ先に疑う',
              '学習データには賞味期限がある。バージョン依存情報は公式ドキュメントで確認',
              'SQLインジェクション・ハードコードされた秘密情報・入力無検証に特に注意',
              'AIはコパイロット。コードの最終責任者はあなた自身',
            ],
            quiz: {
              question:
                '生成AIに「Node.js で MySQL にクエリを投げる関数を書いて」と頼んだら以下のコードが返ってきた。最も重大な問題は？',
              hint: 'userId がブラウザのフォームから来る場合、ユーザーが任意の文字列を入れられます。',
              options: [
                {
                  text: '変数名 `conn` より `connection` にすべき',
                  correct: false,
                  why: '変数名の長さはコーディングスタイルの問題で、セキュリティには直接関係しません。',
                },
                {
                  text: 'SQLを文字列結合で組み立てているため、SQLインジェクション攻撃を受ける危険がある',
                  correct: true,
                  why: '正解！`"WHERE id = " + userId` のように直接結合すると、userId に `1 OR 1=1` などを入れられ全データが取れてしまいます。プレースホルダー（`conn.query("SELECT * FROM users WHERE id = ?", [userId])`）を使うのが鉄則です。',
                },
                {
                  text: 'async/await を使っていないのでエラーハンドリングができない',
                  correct: false,
                  why: 'async/awaitはPromise処理を読みやすくしますが、コールバックスタイルでもエラーハンドリングは可能です。このコードの最大問題はSQLインジェクション脆弱性です。',
                },
              ],
            },
            codeExercise: {
              prompt: `AIが生成した「ユーザー情報取得API」のコードです。
セキュリティの観点で問題を見つけ、最も重大なリスクを選んでください。`,
              code: `/* === AIが生成したコード（疑似出力）=== */

// Node.js + mysql2 によるユーザー情報取得
async function getUserById(userId) {
  // ① SQLを文字列結合で生成
  const sql = "SELECT * FROM users WHERE id = " + userId

  try {
    const [rows] = await conn.query(sql)
    if (rows.length === 0) {
      return { error: 'User not found' }
    }
    return rows[0]
  } catch (err) {
    // ② エラー情報をそのまま返している
    return {
      error: err.message,
      stack: err.stack,
      query: sql,
    }
  }
}

// APIキーの設定
const DB_PASSWORD = 'mySecretPass123'  // ③ パスワードが直書き
const conn = mysql.createConnection({
  host: 'localhost',
  user: 'root',
  password: DB_PASSWORD,
  database: 'myapp'
})`,
              question:
                'このコードに3つのセキュリティ問題が含まれています。「最も即座に悪用される危険性が高い」問題はどれ？',
              hint: '攻撃者が外部から「入力を操作」できる問題に注目してください。',
              options: [
                {
                  text: '③ DBパスワードが直書きされている（ハードコード）',
                  correct: false,
                  why: 'DBパスワードのハードコードは重大な問題ですが、これはコードを読めた場合のリスクです。外部から直接悪用するには一定の条件が必要です。',
                },
                {
                  text: '① SQL文字列結合によるSQLインジェクション（外部入力が直接SQLに展開される）',
                  correct: true,
                  why: '正解！これが最も直接的かつ即座に悪用できるリスクです。`userId` に `1 OR 1=1 --` を渡すだけで全ユーザーデータが漏洩します。APIのエンドポイントにリクエストを送れる人なら誰でも実行できます。プレースホルダー `conn.query("... WHERE id = ?", [userId])` で対処します。',
                },
                {
                  text: '② エラーのスタックトレースとSQLをレスポンスに含めている',
                  correct: false,
                  why: 'これも重大です（攻撃者にDB構造のヒントを与える）が、SQLインジェクションは「攻撃者が自分で入力を送ればいい」ため即効性が高いです。エラー露出は「SQLインジェクションが成功した後」にダメージを増やす脆弱性です。',
                },
              ],
            },
          },
        ],
      },
    ],
  },
]
